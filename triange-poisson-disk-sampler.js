class TrianglePoissonDiskSampler extends PoissonDiskSampler {
  constructor(width, height, minimumDistance, samplesBeforeRejection, onPointAdded) {
    super(width, height, minimumDistance, samplesBeforeRejection, onPointAdded);
  }

  async generatePoints() {
    
  }
}
